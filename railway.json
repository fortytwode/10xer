{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "node src/index.js --server-name $SERVER_NAME --sse-url $SSE_URL --api-key $API_KEY"
  }
}
