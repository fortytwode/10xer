{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "node src/index.js --fb-token your_actual_facebook_access_token --expires-in 3600",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
