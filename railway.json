{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd oauth-relay-service && npm install"
  },
  "deploy": {
    "startCommand": "cd oauth-relay-service && npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
