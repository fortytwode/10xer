# OAuth Relay Service Configuration
# Uncomment and configure these variables to enable relay-based OAuth

# OAuth Relay Service URL (set this to enable relay OAuth)
# OAUTH_RELAY_URL=https://your-relay-service.railway.app

# Facebook App Configuration (update with your live app)
# FACEBOOK_APP_ID=your_live_facebook_app_id
# FACEBOOK_APP_SECRET=your_facebook_app_secret

# MCP Server Configuration
MCP_SERVER_NAME=facebook-ads-mcp
MCP_SERVER_VERSION=1.0.0

# Note: When OAUTH_RELAY_URL is set, the MCP server will use the relay service
# for OAuth authentication. When not set, it falls back to direct OAuth flow.
